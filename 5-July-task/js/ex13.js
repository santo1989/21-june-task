function myFunction13() {

    const multiply = (a,b) => {
        const product = a*b;
       return result = console.log(`The product of ${a} and ${b} is ${product}`);
      }
      
      multiply(2,3);
     // The product of 2 and 3 is 6
    
}